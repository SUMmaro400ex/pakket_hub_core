require 'spec_helper'

describe PakketHubCore do
  it 'has a version number' do
    expect(PakketHubCore::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
